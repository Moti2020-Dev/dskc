# DeepSeek python Terminal Client
---
Use:
`pip install -r requirements.txt`
To install all the requirements.

## How to use:

`python3 main.py`
Debug flag: `--debug`
Test flag: `--test`

Create a **chat** if you dont have any in the recent history.